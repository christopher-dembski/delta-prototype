package app;

// doesn't do anything right now,
// but some of the methods form AppView should probably be moved in here
// the view model is responsible for fetching data and managing state,
// while the view is just responsible for the actual appearance
// i.e. making things appear on the page
public class AppViewModel {
    protected AppViewModel() {
    }
}
